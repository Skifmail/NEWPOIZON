# ============================================
# ЛОКАЛЬНАЯ РАЗРАБОТКА (не для продакшена!)
# ============================================
# Скопируйте этот файл как .env.local и используйте для локального тестирования

# Режим отладки Flask (True для разработки, False для production)
WEB_APP_DEBUG=True

# Отключаем Celery для локального тестирования (используется threading fallback)
# На продакшене эта переменная НЕ должна быть установлена!
DISABLE_CELERY=True
