# ============================================================================
# ЗАВИСИМОСТИ ПРОЕКТА: Poizon → WordPress Сервис
# ============================================================================
# Этот файл содержит список всех необходимых Python библиотек для работы
# приложения синхронизации товаров Poizon → WordPress
# 
# Установка: pip install -r requirements.txt
# ============================================================================

# --- Веб-фреймворк ---
# Flask используется для создания веб-интерфейса и REST API endpoints
flask==3.0.0                    # Легковесный веб-фреймворк, обработка HTTP
gunicorn==21.2.0                # WSGI-сервер для продакшена (VPS)

# --- Работа с HTTP и API ---
# Requests - основная библиотека для всех API запросов (Poizon, WooCommerce)
requests==2.31.0                # HTTP клиент для API запросов
urllib3==2.1.0                  # Low-level HTTP библиотека (зависимость requests)

# --- OpenAI API ---
# OpenAI для генерации SEO-описаний товаров через GPT
openai==1.54.0                  # OpenAI API клиент для GPT-4
httpx<0.28                      # HTTP клиент для OpenAI (0.28+ имеет breaking changes)

# --- Обработка данных ---
# Pandas используется для манипуляции данными товаров и экспорта в CSV
pandas==2.3.3                   # Обработка табличных данных, DataFrame операции

# --- Обработка изображений ---
# Pillow для изменения размера изображений до 600x600 с сохранением пропорций
Pillow==10.4.0                  # Обработка и ресайз изображений товаров

# --- Парсинг контента ---
# BeautifulSoup для парсинга HTML описаний товаров (если потребуется)
beautifulsoup4==4.12.3          # Парсинг HTML/XML контента

# --- Конфигурация ---
# Python-dotenv для безопасного хранения API ключей в .env файле
python-dotenv==1.0.1            # Загрузка переменных окружения из .env

# --- Визуализация прогресса ---
# TQDM показывает прогресс-бары при длительных операциях загрузки
tqdm==4.67.1                    # Прогресс-бары в консоли и логах

# --- Сборка EXE (опционально) ---
pyinstaller==6.10.0             # Создание исполняемых файлов

# --- Инструменты разработки (опционально) ---
pytest==8.3.5                   # Тестирование
pytest-cov==6.0.0               # Покрытие тестами
black==24.8.0                   # Форматирование кода
flake8==7.1.1                   # Линтер
mypy==1.13.0                    # Проверка типов

# ============================================================================
# НОВЫЕ ЗАВИСИМОСТИ ДЛЯ АРХИТЕКТУРНЫХ УЛУЧШЕНИЙ
# ============================================================================

# --- Кеширование и очереди задач ---
redis==5.0.1                    # Redis клиент для кеширования и Celery broker
celery==5.5.3                   # Фоновые задачи и периодические джобы

# --- Асинхронные запросы ---
aiohttp==3.13.2                 # Асинхронный HTTP клиент для параллельных запросов
aiodns==3.1.1                   # DNS resolver для aiohttp (ускорение)

# --- Мониторинг (опционально) ---
prometheus-client==0.21.0       # Метрики для мониторинга
loguru==0.7.3                   # Продвинутое логирование

# ============================================================================
# ПРИМЕЧАНИЕ:
# - Все версии зафиксированы для стабильной работы
# - Не меняйте версии без необходимости
# - При ошибках установки попробуйте: pip install --upgrade pip
# - Для production рекомендуется использовать Redis для кеширования
# ============================================================================
